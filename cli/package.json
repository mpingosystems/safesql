{
  "name": "safesql",
  "version": "0.4.0",
  "description": "Pre-execution semantic SQL validation — CLI",
  "type": "module",
  "bin": { "safesql": "./dist/index.js" },
  "scripts": {
    "build": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "commander": "^12",
    "chalk": "^5"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "@types/node": "^24.12.2"
  }
}
